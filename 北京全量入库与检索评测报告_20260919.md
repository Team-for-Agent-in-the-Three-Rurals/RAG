# 北京数据全量入库与检索评测报告

**项目**：三农智能体 —— 成员二王乐涵Lehan（RAG、Milvus 与可信引用）
**环境**：SCNet 华东一区【昆山】Notebook 实例（2× 海光 DCU K100-AI，12 核 / 230GB 内存）
**代码版本**：rag_member2_latest（09-04 验收版，含成员一全部 4 项修复）

---

## 一、全量入库结果 ✅

| 指标 | 数值 |
|---|---|
| 源数据 | `beijing.jsonl`（160,164,228 字节，与本地 MD5 一致） |
| 处理文档 | **5,849 篇**（6,238 行 − 389 条标签过滤记录） |
| 原始 chunk | 186,014 |
| 超长 chunk 二次切分 | 253 |
| **安全 chunk 总数** | **186,608** |
| **成功插入** | **186,608（100%，零失败）** |
| Collection | `policy_chunks_bge_m3_beijing_v1` |
| 存储 | Milvus Lite，`/public/home/scdaihao/milvus_lite_db/beijing_v1.db`（共享存储 1T，973G 可用） |
| 总耗时 | 约 45 分钟（6 个文档批次） |

**入库参数**（重要——DCU 环境必读）：

```
RAG_MILVUS_URI=/public/home/scdaihao/milvus_lite_db/beijing_v1.db
RAG_EMBEDDING_MAX_LENGTH=4096    # 首次用默认 8192 在批次2 OOM（单次申请 62GB/卡共64GB），降至 4096 解决
PYTORCH_HIP_ALLOC_CONF=expandable_segments:True
--batch-size 16 --insert-batch-size 1000 --drop-existing
```

> 中文 chunk（≤7600 字节）实际约 2500–3800 token，4096 截断不损失有效信息；降参后速度反而更快（约 79 chunk/秒）。

---

## 二、全量库完整性验证 ✅

- `row_count = 186,608`，与插入数**完全一致，零丢失**
- 35 个元数据字段全部就位（含 `valid_from / valid_to / is_currently_valid / is_abolished` 等时效性字段、`province / city / county / region_code` 地区字段，支撑后续地区+时效过滤）
- 抽样检索（3 问）全部命中高相关结果：

| 查询 | Top-1 结果 |
|---|---|
| 北京市高标准农田建设的补助标准是多少 | 《北京市实际种粮农民一次性补贴实施方案》补贴标准条款（score 0.30） |
| 东城区行政事业单位国有资产管理办法适用于哪些单位 | **精确命中目标文档** doc_id=110000-110000-110101-000002 |
| 农村集体资产管理的公开要求有哪些 | "农村集体资产管理坚持民主、公开的原则"条款（score 0.26） |

---

## 三、20 问检索评测（Recall@5）

| 模式 | Recall@5 | 命中 / 总数 |
|---|---|---|
| 向量检索（vector） | **70%** | 14 / 20 |
| 混合检索（hybrid：向量+关键词 RRF） | **70%** | 14 / 20 |

评测配置：`--top-k 5`，评测集 `evaluation_questions_test_local.json`（基于北京东城区政策构造，每题带 target_doc_id）。
报告文件：`/public/home/scdaihao/eval_beijing_v1_vector.json`、`eval_beijing_v1_hybrid.json`

### 未命中分析（6 题，两种模式完全相同）

| 题号 | 查询 | 未命中原因归类 |
|---|---|---|
| q07 | 如何加强职业培训并促进就业？ | 泛化查询 |
| q09 | 慢性病综合防控工作有哪些重点任务？ | 泛化查询 |
| q11 | 声环境区划实施细则的适用范围是什么？ | 泛化查询 |
| q13 | 食品药品安全委员会更名后承担什么职责？ | 泛化查询 |
| q15 | 企业节能改造可以获得哪些政策支持？ | 泛化查询 |
| q20 | 网格化环境监管如何划分责任和开展巡查？ | 泛化查询 |

**根因**：这 6 题均为**泛化型查询**（不含"东城区"等地区限定词），目标文档是东城区政策；在全北京 18.6 万 chunk 的库中，市级或其他区存在语义更相似的同主题政策文档，占据了 Top-5。此前 12 篇小库测试时这些题全部命中（当时无竞争文档）。

**结论**：属评测题设计特点，而非检索系统缺陷。实际使用中用户查询通常带地区/事项限定词（如 q03、q04 等 14 题均命中）。

**改进方向（可选，不阻塞验收）**：
1. 评测题补充地区限定词后复测（预期 Recall 明显提升）；
2. 检索链路开启 `region` 元数据过滤（schema 已具备字段）；
3. 对泛化查询引入"同主题多地区聚合返回"策略。

---

## 四、已具备的能力清单（对应四阶段交付）

1. ✅ **知识库元数据**：35 字段 schema，5,849 篇北京政策全量入库，含地区与时效性字段
2. ✅ **检索能力**：向量检索 + 混合检索（向量+关键词 RRF）均已验证可用
3. 🔶 **可信引用**：检索结果携带 doc_id / title / doc_number / source_url / publish_org 等完整溯源字段（引用样例可从本次评测结果直接生成）
4. ⬜ **联调**：待成员 1/4/5 接入（外部访问需走 REST，平台反代挡 gRPC）

---

## 五、遗留问题与建议

| 事项 | 状态 | 建议 |
|---|---|---|
| Milvus Standalone 启动 | ❌ 2.6.18 deb 多次 panic | 向工程师索取其内部测试跑通的 milvus.yaml 及部署步骤 |
| 全国 31 省入库（18G / 约 1,177 万 chunk） | ⬜ 未开始 | Milvus Lite 不适用此量级，需 Standalone 或分区方案 |
| 团队账户 achfss25u0 欠费 | ⚠️ 未解决 | 提醒管理员李大鹏充值 |
| 外部成员连库 | ⬜ 未开始 | 平台反代挡 gRPC，需自建 REST/HTTP 服务层 |

---

## 附：关键命令记录

```bash
# 全量入库（DCU 安全参数）
cd /public/home/scdaihao/rag_member2_latest
export RAG_MILVUS_URI=/public/home/scdaihao/milvus_lite_db/beijing_v1.db \
       HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 \
       RAG_EMBEDDING_MAX_LENGTH=4096 PYTORCH_HIP_ALLOC_CONF=expandable_segments:True
nohup python ingest.py /public/home/scdaihao/RAG/member2_20260823/data/beijing.jsonl \
  --batch-size 16 --insert-batch-size 1000 \
  --collection-name policy_chunks_bge_m3_beijing_v1 --drop-existing \
  > /public/home/scdaihao/ingest_beijing_full2.log 2>&1 &

# 20 问评测
python evaluate_retrieval.py --collection-name policy_chunks_bge_m3_beijing_v1 \
  --questions evaluation_questions_test_local.json --top-k 5 \
  --mode hybrid --output /public/home/scdaihao/eval_beijing_v1_hybrid.json
```
